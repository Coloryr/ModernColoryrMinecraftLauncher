{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all launcher windows",
  "windows": ["main", "mcml-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default"
  ]
}
